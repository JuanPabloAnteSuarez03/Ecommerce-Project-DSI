import React from 'react';

// ==============================|| NAV LEFT ||============================== //

const NavLeft = () => {
  return (
    <React.Fragment>
      {/* Content removed as search functionality is no longer needed */}
    </React.Fragment>
  );
};

export default NavLeft;
